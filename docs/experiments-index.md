# Experiments index

1 row per directory under `experiments/`, the question its README states, where
its output lands, and the date its README (or, where there is none, its
script) was last written. Directories with no README are marked, and their
question is taken from their script's docstring instead. 47 to 48 of the 51
directories were added or substantially rewritten on 2026-09-11 during the
reorganisation, so most dates below cluster there. See `experiments/README.md`
for the package rule these directories follow.

| directory | question | results | date |
|---|---|---|---|
| `adaptive_attack` | Whether an evasive checkpoint trained with the hinge penalty against 1 probe suppresses detection there and how far that suppression transfers to other operators. | `results/adaptive_attacker_analysis.json` | 2026-09-07 |
| `adaptive_defender` | How a defender identifies and still detects an adaptive attacker's evasive checkpoints, given per-operator validation statistics and a sigma-curve reading. | `results/adaptive_defender_analysis.json`, protocol in `docs/results/adaptive-defender-protocol.md` | 2026-09-07 |
| `adversarial_direction` | Whether Karayalcin, Krcek, Chen and Picek's PGD-to-target-class findings for ViT-B/16 on CIFAR-100 replicate on this project's own checkpoints. | `results/_experiments/adversarial_direction/<folder>.json` | 2026-09-11 |
| `all_to_all_entropy` | Whether the prediction entropy PSBD's cache already holds covers the all-to-all case that shift-based detection misses. | `results/all_to_all_entropy.json` | 2026-09-08 |
| `all_to_all_occlusion` | Whether a spatially selective occlusion probe detects an all-to-all backdoor that dropout-based PSBD cannot. | stdout only, no results file written | 2026-09-08 |
| `attack_viability` | Which combinations of attack, dataset and poison rate actually produced a firing backdoor before any detection sweep is run against them. | reads existing `metrics.json` files, writes no new file | 2026-09-07 |
| `attention_heads` | Whether a small number of attention heads route the trigger, measured by attention divergence and entropy, and whether those heads give a usable per-sample detection feature. | `results/attention_divergence.json`, `results/attention_entropy.json` | 2026-09-07 |
| `backdoor_direction` | 5 measurable properties the companion paper's residual-stream-direction account predicts, tested on the same extracted CLS features. | `results/direction_persistence.json` and 4 sibling files | 2026-09-07 |
| `backdoor_direction_layers` | Whether the layer at which each attack's backdoor direction reaches the CLS token predicts which dropout placement works best for that attack. | `results/<folder>/direction_layers_<reduction>.json` | 2026-09-07 |
| `backdoor_neurons` | Which layers carry each attack's backdoor, whether attacks share a layer, whether SAM training moves it, and whether it is a set of neurons or a direction. | `results/<folder>/backdoor_neurons.json` | 2026-09-07 |
| `balanced_panels` | Whether 4 previously reported conclusions still hold once every comparison is rebuilt on the same coverage-balanced set of checkpoints instead of whatever happened to exist. | reads and cross-checks `results/` in place | 2026-09-07 |
| `cache_integrity` | Whether the stage-1 cache under `results/<folder>/psbd/` that every headline number is computed from is internally consistent, with no truncated tensor, no manifest mismatch and no probe that silently never fired. | `results/_experiments/cache_integrity/cache_integrity.json` | 2026-09-11 |
| `content_dependence` | Not yet populated. No README and no script. | none | directory created 2026-09-08, empty |
| `decision_leverage` | Whether a sample's decision margin under the classification head alone, with no perturbation at all, carries as much detection signal as PSBD's stochastic probe. | no README, output path not yet fixed in `measure.py` | 2026-09-11 |
| `detector_ensemble` | Whether averaging a sample's PSU rank across every placement, since no single placement wins on every attack, beats committing to 1. | discussed in `docs/results/adaptive-defender-protocol.md` | 2026-09-07 |
| `dropout_kills_direction` | Whether dropout destroys the clean-prediction evidence in the residual stream while leaving the trigger-to-target path intact, the representational claim behind PSBD's PSU statistic. | `results/<folder>/dropout_direction_survival.json` | 2026-09-07 |
| `gaussian_batch_coupling` | Whether scaling Gaussian noise by a batch-wide standard deviation biased the comparison between the validation, clean and backdoor splits, which hold different image populations. | `results/gaussian_batch_coupling.json` | 2026-09-07 |
| `gradient_weight_ablation` | Whether a triggered prediction rests on fewer classification-head weights than a clean prediction, measured by gradient-directed per-sample weight ablation rather than random activation noise. | no README, `measure.py` has no fixed default output path yet | 2026-09-11 |
| `head_profile` | Whether the shape of a per-head sensitivity profile, not its magnitude, is the backdoor signature, and whether masking the 3 heads H31 named beats masking random ones. | `results/targeted_head_psbd.json` | 2026-09-07 |
| `latent_geometry_predicts_detection` | Whether a rank-ratio statistic computed with no perturbation at all, read purely from the model's feature geometry, predicts how well a perturbation-based detector will do. | reads `results/detection_summary.csv`, writes alongside it | 2026-09-07 |
| `low_fpr_audit` | What PSBD delivers at a false-positive budget a defender would actually deploy at, since AUROC integrates over regions nobody uses. | `results/low_fpr_audit.json` | 2026-09-08 |
| `median_rank_union` | Whether a median-rank union across probes, needing a majority of probes to be wrong, resists an attacker who inverts the probed placement rather than just neutralizing it, better than the min-rank union does. | `--results-dir results`, output path set by `--output` at call time | 2026-09-07 |
| `monte_carlo_passes` | Whether raising the Monte Carlo pass count k helps detection more at 1% poisoning than at 10%, which would show the 1% failure is estimator noise rather than a missing signal. | no fixed output file found in the script | 2026-09-07 |
| `multi_probe` | Whether a min-rank combined score across every available perturbation operator flags an evasive checkpoint that any single probed operator misses. | `results/multi_probe_analysis.json` | 2026-09-07 |
| `operator_ranking` | Whether PSBD's published configuration collapsing at 1% poisoning is a limit of prediction-shift detection itself or a bad operating point, read from 1 cached surface by 7 scripts that must agree on what a measurement means. | `docs/results/operator-position-ranking.md`, `docs/results/detection-operating-points.md` | 2026-09-07 |
| `prediction_depth` | At what depth and from which patches a ViT's answer is already decided, a question about representation rather than robustness. | `results/<folder>/prediction_depth.json` | 2026-09-09 |
| `preflight` | Which judging rule a synthetic sign gate applies to decide whether a detector's result on a constructed backdoor counts as exercised, not exercised, or not judgeable at all. | shared library, no results file of its own | 2026-09-11 |
| `pretrained_interpolation` | Whether interpolating a checkpoint's weights back toward its public pretrained initialization gives a detection signal from outside the activation-noise operator family entirely. | no README, `measure.py` has no fixed default output path yet | 2026-09-11 |
| `probe_fusion` | Whether combining 2 placements that fail on different attacks covers both, making the deployment configuration a set of probes rather than 1. | reads `results/<folder>/psbd/<placement>/rate_*.pt` | 2026-09-09 |
| `probe_union` | Whether the min-rank multi-probe union built against an adaptive attacker also helps, hurts or does nothing on the 65 ordinary checkpoints the paper's headline reads, including the 2 inverted checkpoints it names. | `results/_experiments/probe_union/probe_union.json` | 2026-09-11 |
| `psbd_cost` | What running PSBD at k = 3 forward passes costs against 1 unperturbed pass, and whether raising k to 5, 10 or 20 buys anything worth the wall-clock time on this project's hardware. | `results/_experiments/psbd_cost/cost.json` | 2026-09-11 |
| `psu_vs_confidence` | Whether PSU is just an elaborate proxy for baseline confidence, since a prediction starting near probability 1 has more room to fall under dropout than one starting near 0.6. | writes a JSON report keyed by placement | 2026-09-07 |
| `rademacher_probe_variance` | Whether a Rademacher perturbation reduces the prediction-shift estimator's variance against Gaussian noise, and whether that reduction improves detection specifically at low pass counts. | no README, output path set in `measure.py` | 2026-09-11 |
| `removal_defences` | Whether the backdoor can be removed from a trained checkpoint, rather than just detected, using what the direction results established, scored by attack success and clean accuracy against a random-direction control. | `results/direction_erasure.json`, `results/skip_scaling.json` | 2026-09-07 |
| `residual_stream_mechanism` | What the residual stream is doing at PSBD's own operating point that makes 1 injection point beat another, specifically whether LayerNorm absorbs 1 operator's disturbance and not another's. | `--results-dir results`, 1 JSON report per script (`identity_attention.py`, `layernorm_absorption.py`, `cls_routing.py`, `logit_attribution.py`) | 2026-09-09 |
| `retention_curves` | Whether the backdoor is the robust path under every perturbation modality tried so far, activation dropout, gradient-guided weight ablation, weight interpolation and prediction depth, or only under some. | reads across the 4 probes above, writes no new cache | 2026-09-09 |
| `sam_backdoor_effect` | The positive control for whether SAM training amplifies the backdoor on ViT beyond what a prediction-space detector can reach, or does not amplify it at all. | `results/sam_backdoor_effect.json` | 2026-09-07 |
| `sam_low_rate` | Whether the small SAM gain for PSBD found when pooling every poison rate together still holds at the 1% and 5% rates an attacker would actually use. | `results/_experiments/sam_low_rate/sam_low_rate.json` | 2026-09-11 |
| `sam_mechanism` | Whether SAM amplifies the trigger's footprint in the network or instead widens the margin PSBD reads, testing the 3 amplification metrics the SAM paper proposes on this project's own ViT-B/16 checkpoints. | `results/_experiments/sam_mechanism/<pair>.json` | 2026-09-11 |
| `sam_reading` | Whether training the victim with sharpness-aware minimisation helps PSBD detect the backdoor on ViT and Swin, matched combination for combination against Adam. | `results/_experiments/sam_reading/sam_reading.json` | 2026-09-11 |
| `sam_training_set_detection` | Whether SAM training widens the penultimate-layer gap between poisoned and clean features enough to make off-the-shelf training-set poison detectors more effective, as the SAM paper claims. | `results/_experiments/sam_training_set_detection/<dataset>_<attack>_<rate>.json` | 2026-09-11 |
| `shift_in_latent_space` | Whether a shifted clean prediction under dropout moves in representation space the way PSBD's stated mechanism requires, not just in which label it lands on. | `results/<folder>/shift_latent.json` | 2026-09-07 |
| `stealth` | Whether the attacks in the evaluation set are comparably visible to a human, since detection numbers are only comparable across attacks once a stealth axis is established. | `docs/results/stealth-metrics.md` is the published record | 2026-09-09 |
| `style_content_split` | Whether a backdoor trigger behaves as a separable style feature under a style-content decomposition, as opposed to being entangled with the class-relevant structure. | writes a JSON report under `--results-dir` | 2026-09-08 |
| `tail_selection` | Whether a rule that reads no ground-truth labels can select the detection tail, given only a clean validation split and a mostly-clean suspicious pool. | `--results-dir results` | 2026-09-07 |
| `theory_predictions` | Whether 3 falsifiable predictions that follow from reframing PSBD as curvature estimation hold against data already cached on disk, with no retraining and no new forward pass. | reads `results/detection_summary.csv` and `results/<folder>/psbd/` | 2026-09-07 |
| `token_structure` | Whether projecting per-token features onto the backdoor direction localizes a local trigger's position and whether the resulting concentration statistic classifies the attack family. | `results/token_localization.json` | 2026-09-07 |
| `wanet_cifar10_audit` | Whether a single checkpoint's 0.459 AUROC reading for the recommended placement against WaNet on CIFAR-10 at 10% poisoning is real, a sweep artefact or a property of that 1 checkpoint. | `results/vit_cifar10_wanet_0_1/psbd_metrics.json`, `results/bb_cifar10_wanet_0_1/` | 2026-09-11 |
| `weight_detector` | Whether Karayalcin, Krcek, Chen and Picek's data-free weight-only detector, which needs no forward pass and no clean data, flags WaNet and BPP reliably on this project's checkpoints the way they report. | `results/_experiments/weight_detector/summary.json` | 2026-09-11 |
| `weight_structure` | Whether the backdoor is visible in the trained weights themselves as a low-rank perturbation, subtracting a benign model from its matched backdoored twin and reading the singular value spectrum. | `results/weight_spectral_signature.json` | 2026-09-07 |
| `whole_network_erasure` | Whether projecting the backdoor direction out of every residual-writing matrix in the network, not just the last 2 blocks, reproduces Karayalcin et al.'s reported attack-success collapse from 97.7 to 6.7. | no README, output path set in `measure.py` | 2026-09-11 |
