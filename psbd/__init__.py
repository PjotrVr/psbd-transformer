"""Prediction Shift Backdoor Detection for Vision Transformers.

Every caller imports a specific submodule (``from psbd.config import
DATASET_REGISTRY``). This package deliberately re-exports nothing, so the module
a symbol lives in stays visible at the point of use.
"""
