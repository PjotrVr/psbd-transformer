"""Latent-space analysis tools: TAC, backdoor direction, CKA, PCA, UMAP, Lipschitz.

Import the specific submodule you need explicitly, for example
``from psbd.analysis.cka import debiased_linear_cka``. This package deliberately
does not re-export its submodules, which keeps the module a symbol lives in
visible at the point of use.
"""
