"""Helpers with no subject of their own.

numerics is division that fails loudly where a denominator vanishes. provenance
is the git and clock stamps an artefact writer records so a number traces to a
commit. Import the specific submodule you need explicitly, for example
`from utils.numerics import safe_ratio`. This package re-exports nothing.
"""
