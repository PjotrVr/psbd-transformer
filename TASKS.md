I need to create a smaller smoke tests.
Current smoke test runs one epoch on the whole dataset for both training backdoor and training benign and evaluating.
I need it to have some specification like "smoke_test" for 1 epoch or for 1batch somehow. how could we do that?
So that we have faster and smaller smoke tests. 