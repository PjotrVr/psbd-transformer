        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
P        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
B        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
I        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
F        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
P        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
U        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
W        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
P        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
Y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
H        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
P        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
H        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
#        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
q        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
4        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
F        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
4        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
#        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
q        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
4        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
Y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
H        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
#        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
q        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
4        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
#        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
q        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
4        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
B        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
I        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
H        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
L        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
F        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
5        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
>        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
|        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
'        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
>        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
>        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
H        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
L        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
F        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
*        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
>        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
P        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
/        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
\        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
\        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
\        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
\        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
P        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
U        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
\        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
>        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
z        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
6        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
+        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
H        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
+        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
Y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
9        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
9        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
5        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
B        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
I        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
%        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
B        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
I        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
z        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
3        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
*        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
*        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
z        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
3        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
*        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
*        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
z        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
6        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
S        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
D        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
9        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
P        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
'        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
'        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
'        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
'        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
K        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
2        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
9        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
9        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
x        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
0        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
[        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
1        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
]        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
-        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
C        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
w        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
h        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
d        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
G        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
N        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
E        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
A        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
T        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
O        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
R        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
u        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
c        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
l        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
y        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
+        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
j        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
r        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
v        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
o        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
.        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
t        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
+        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
,        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
{        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
b        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
g        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
p        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
k        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
s        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
}        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
f        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
e        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
=        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
_        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
"        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
:        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
         caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
m        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
a        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
i        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
n        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
(        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
)        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),

        caption=(
            f"Per-layer profile of the backdoor direction on {len(records)} ViT-B/16 models, "
            f"{record['samples']} paired images each."
        ),
