\documentclass[../main.tex]{subfiles}
\begin{document}

\section{The placement basis}
\label{app:basis}

\Cref{tab:basis-declaration} reprints the declaration every panel cell was swept with, and \cref{tab:basis-ranking} ranks all \BasisSize{} placements on the cached cells. The spread between the best and the worst placement at the adaptive rule is \BasisSpreadAdaptive{}, from \code{\BasisBestPlacement} at \BasisBestAurocAdaptive{} to \code{\BasisWorstPlacement} at \BasisWorstAurocAdaptive{}, and the published placement is ranked \BasisRankPublishedAdaptive{}.

\input{tables/basis_declaration}
\input{tables/basis_ranking}

\section{Supplementary tables}

\Cref{tab:family-split-confounded} is the family gap read with the declaration's operator-mixing family tags. \Cref{tab:mech-operator-agreement} is the per-placement agreement with the recommended placement. \Cref{tab:mech-shift-target-tl1} is the target-share reading at a second target class, empty until the GTSRB clean-label cells carry a cache.

\input{tables/family_split_confounded}
\input{tables/mech_operator_agreement}
\input{tables/mech_shift_target_tl1}

\section{The hypothesis ledger}
\label{app:hypotheses}

Every question this project asked is a file with a falsifiable prediction, the evidence and a verdict, and a refuted question keeps its file. \Cref{tab:hypothesis-ledger} reprints the index of all \HypothesisCount{} with the leading verdict of each, \HypothesisSupported{} supported or confirmed, \HypothesisRefuted{} refuted, \HypothesisPartial{} partial and \HypothesisInconclusive{} inconclusive.

\input{tables/hypothesis_ledger}

\end{document}
