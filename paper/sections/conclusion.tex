\section{Conclusion}
\label{sec:conclusion}

PSBD transfers to a Vision Transformer once its perturbation is placed where a ViT carries a backdoor. Dropout on the residual stream, our reading of the original placement, ranks \DetectorsAurocRankPublished{} of \DetectorsAurocDefensesRanked{} defenses. Masking whole tokens at the attention input, PSBD-TM, reaches a mean AUROC of \HeadlineAurocAdaptive{} on \PanelCellsCached{} ViT-B/16 models and \SwinRecommendedAurocAdaptive{} on \SwinCells{} Swin-S models, and gains most at \PanelRateLow{} poisoning. It removes the trigger's tokens before attention routes them to the class token, where the backdoor's direction is written late, and a LayerNorm absorbs noise but not masking, which is why the operator matters as much as the site.

A practitioner on a ViT or a Swin should run PSBD with token masking at the attention input, choose the rate by the clean shift ratio as the original does, and combine several placements against a knowledgeable attacker. The result is a tie with the calibrated IBD-PSC port, \DetectorsAurocMargin{} ahead on AUROC and \DetectorsTprTwoZeroMargin{} at a 20\% budget, not a new state of the art. An attacker who trains against the union of probes, and the all-to-all attack, which needs a different statistic, remain open.
