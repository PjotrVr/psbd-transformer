\section{Where the backdoor lives in a ViT}
\label{sec:mechanism}

\Cref{sec:results} is a ranking. This section explains it by following a backdoor through a ViT, each experiment on the same backdoored models with the benign model of the same dataset, probed with the same trigger, as the control. \Cref{app:picek} compares the picture with Karayal\c{c}{\i}n et al.~\cite{karayalcin2026backdoor} in detail.

\subsection{A direction, written late}
\label{sec:mechanism:direction}

For a model and a layer, the backdoor direction is the mean difference between the class token's activation on a triggered image and on its clean copy, the construction Karayal\c{c}{\i}n et al. use on ViTs. \Cref{tab:direction-ablation} tests whether it is causal. Projecting the final layer's direction out of the class token takes attack success to \AblationDirectionMinAsr{} on BadNets and to at most \AblationDirectionMaxAsr{} on any of the \AblationCheckpoints{} attacks, while clean accuracy falls by at most \AblationCleanAccuracyCost{}. Projecting out a random direction of the same norm leaves attack success at \AblationRandomDirectionMinAsr{} or above, and so does zeroing the 20 coordinates the trigger moves most. The backdoor is a direction in the residual stream rather than a set of neurons, so a method that ranks coordinates, heads or channels sees only a rotated shadow of it. LF, the exception at the final layer, falls to \ErasureLfAsrAfterAllWritesMax{} once the direction is removed from every block's write to the stream. The direction stays near the benign level through the first half of the network and is written in the last third (\cref{app:mechanism}).

\input{tables/direction_ablation}

\subsection{Routed through attention from the trigger's own tokens}
\label{sec:mechanism:routing}

Activation patching \cite{zhang2024patching} asks where the backdoor sits at each depth: at a chosen block we overwrite a group of tokens on the triggered image with their clean values and measure how much of the clean prediction comes back (\cref{fig:patching}). For the local triggers, patching the trigger's own tokens recovers \PatchingLocalEarlyTrigger{} of the clean prediction over blocks \PatchingEarlyLayers{}, and the class token overtakes them only at block \PatchingLocalCrossover{} on average. A random group of the same size never exceeds \PatchingRandomNullMax{}. The class token's attention on the trigger's tokens rises from \RoutingBadnetClsEarly{} in blocks 1 to 4 to \RoutingBadnetClsLate{} in blocks 9 to 12 on BadNets, against \RoutingBenignClsLate{} on the benign model. For a local trigger the backdoor therefore sits in the trigger's own tokens for most of the network and reaches the class token through attention only in the last quarter. A global trigger covers every patch, so this test can only confirm that its backdoor is spread over the tokens.

\begin{figure*}[t]
\centering
\includegraphics[width=0.85\textwidth]{mech_activation_patching.pdf}
\caption{Share of the clean prediction recovered when a token group is replaced by its clean value at a given block, averaged over the backdoored models of each attack. For the local triggers (BadNets, TaCT) the trigger's tokens hold the backdoor through block 10 and the class token takes over only at the end.}
\label{fig:patching}
\end{figure*}

\subsection{What this means for the placement}
\label{sec:mechanism:placements}

A backdoored input's decision rests on a few tokens whose content reaches the class token late through attention, and a clean input's decision rests on many tokens. Masking whole tokens at the attention input, before the mixing, removes the trigger's tokens with a probability that grows with the rate, and when it does the shortcut is gone and the prediction moves, while a clean input's remaining tokens still carry its class. The mask must act in every block because the routing can happen at any depth. At the MLP input the same mask acts after attention has already mixed the trigger into the class token, which is the \StaircaseOperatorsBeforeMlpNormTokenMaskGain{} cost of that site in \cref{tab:staircase-operators}. Whole tokens beat coordinates for the same reason. Dropping a share of a token's \VitWidth{} coordinates leaves it present with a noisier description, while masking it removes it from every attention pattern of the block, and channel masking, which removes whole coordinates, trails token masking by \StaircaseOperatorsBeforeAttentionNormChannelMaskGain{}. Dropout on the residual stream perturbs the class token itself, on clean and triggered inputs alike, which is why PSBD-RD reads a coin where the shortcut is weak.

\subsection{LayerNorm absorbs noise and not masking}
\label{sec:mechanism:layernorm}

Every perturbation at the attention input passes through a LayerNorm, which rescales each token to unit variance, so an additive perturbation that raises the variance is partly divided away. \Cref{tab:layernorm-absorption} in \cref{app:mechanism-group} measures it on \AbsorptionModels{} models: the attention input's LayerNorm passes \AbsorptionAttentionInputGaussianSurvival{} of a Gaussian disturbance and \AbsorptionAttentionInputTokenMaskSurvival{} of a token mask, and at the positions no LayerNorm follows every operator survives at \AbsorptionControlSurvival{}. Noise at the attention input is therefore weaker than its rate says and loses \StaircaseOperatorsBeforeAttentionNormGaussianGain{} to masking. Injected after the MLP's LayerNorm the same noise escapes the absorption and reads \StaircaseOperatorsBeforeMlpGaussianAuroc{}, the apparent reversal of \cref{sec:results:operator}.

\subsection{The original account holds for some attacks}
\label{sec:mechanism:phenomenon}

The PSBD paper explains its statistic by neuron bias: under dropout the network falls back on the classes its neurons favor, so clean inputs whose prediction shifts should land on the backdoor's target. Among clean validation images whose prediction changes under PSBD-TM, the mean excess of the target share over the benign reference is \ShiftToTargetExcessAll{}, and it is concentrated in a few attacks. \ShiftToTargetDriftAttacks{} drift toward the target on some dataset, the largest excess \ShiftToTargetLargestExcess{} for \ShiftToTargetLargestCell{}, while \ShiftToTargetNoDriftAttacks{} stay below \ShiftToTargetNoDriftFloor{} on every dataset (\cref{app:shift-target}). The split is not local against global triggers: TaCT is a local patch and does not drift, and LF and BPP are global and do.

Detection works in both regimes, since PSBD-TM detects Blend and TaCT, which show no drift. Neuron bias therefore describes some attacks rather than the statistic's mechanism, and the margin appears to be what makes it work: a triggered input's class has a large margin because the shortcut is firm, and any perturbation that moves clean decisions more than firm ones separates the 2 populations. The margin is not the unperturbed confidence. The maximum softmax probability reads a mean AUROC of \ConfidenceNullAuroc{} over \ConfidenceNullCheckpoints{} models against \ConfidenceNullPsuAuroc{} for PSU, and PSU is ahead on \ConfidenceNullPsuWins{} of them. What the statistic reads is how robust a firm decision is, not how confident it started.
